
CREATE TABLE TEAM (
		ID_team INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  	team_name VARCHAR(30),
  	team_key INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;

CREATE TABLE CLAIM_TYPE (
		ID_claim_type INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
		claim_type VARCHAR(30),
  	ID_team INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;

CREATE TABLE CLIENT (
		ID_client INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
		email VARCHAR(30), 
		name VARCHAR(30), 
		surname VARCHAR(30), 
		birth_date DATE, 
		is_fulltime INTEGER, 
		y_of_experience INTEGER, 
		annual_income INTEGER, 
		addressL1 VARCHAR(30), 
		addressL2 VARCHAR(30), 
		post VARCHAR(10), 
		city VARCHAR(30), 
		country VARCHAR(30), 
		password VARCHAR(30), 
		card_number VARCHAR(70), 
		ccv VARCHAR(30), 
		ID_occupation INTEGER,
  		ID_team INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;
	
CREATE TABLE CLIENTS_CLIENT (
		ID_clients_client INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
		name VARCHAR(30), 
		tax_id VARCHAR(50), 
		risk_contract_percent DOUBLE,
  		ID_team INTEGER NOT NULL)
  	
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;
	
CREATE TABLE CONTRACT (
		ID_contract INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
		contract_value DOUBLE, 
		payment_due_to DATE, 
 		is_paid INTEGER, 
		claim_is_valid INTEGER,
		claim_value DOUBLE,
		claim_date DATE,
		return_account_number VARCHAR(100),
		ID_policy_product INTEGER NOT NULL, 
		ID_clients_client INTEGER, 
  		ID_team INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;

CREATE TABLE FILE (
		ID_file INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
		path VARCHAR(100), 
		ID_contract INTEGER,
	    ID_gear INTEGER,
	    ID_gear_claim INTEGER,
	    ID_sick_day_claim INTEGER,
	    ID_liability_claim INTEGER,
	  	ID_team INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;

CREATE TABLE GEAR (
    	ID_gear INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
		gear_value DECIMAL(20, 2), 
		date_of_purchase DATE, 
		premium_price DOUBLE, 
		ID_policy_product INTEGER NOT NULL, 
		ID_gear_type INTEGER,
  		ID_team INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;


CREATE TABLE GEAR_CLAIM (
		ID_gear_claim INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
		event_date DATE, 
		event_description VARCHAR(100),
		claim_is_valid INTEGER,
		claim_value DOUBLE,
		claim_date DATE,
		return_account_number VARCHAR(100),
		ID_repair_service INTEGER, 
		ID_claim_type INTEGER, 
		ID_gear INTEGER NOT NULL,
  		ID_team INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;

CREATE TABLE GEAR_TYPE (
		ID_gear_type INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
		gear_type VARCHAR(30),
  		ID_team INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;

CREATE TABLE LIABILITY (
		ID_liability INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
		premium_price DOUBLE, 
		max_claim_value DOUBLE, 
		ID_policy_product INTEGER NOT NULL,
  		ID_team INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;

CREATE TABLE LIABILITY_CLAIM (
		ID_liability_claim INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
		description VARCHAR(100),
		claim_is_valid INTEGER,
		claim_value DOUBLE,
		claim_date DATE,
		return_account_number VARCHAR(100),
		ID_liability INTEGER NOT NULL, 
  		ID_team INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;

CREATE TABLE MONTHLY_PAYMENT (
		ID_payment INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
		payment_value DOUBLE, 
		payment_date DATE, 
		ID_client INTEGER NOT NULL,
  		ID_team INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;
	

CREATE TABLE OCCUPATION (
		ID_occupation INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
		occupation VARCHAR(30),
  	ID_team INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;

CREATE TABLE POLICY_PRODUCT (
		ID_policy_product INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
		ID_client INTEGER NOT NULL, 
		ID_product INTEGER NOT NULL, 
		date_from DATE, 
		date_to DATE,
  		ID_team INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;

CREATE TABLE PRODUCT (
		ID_product INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
		product VARCHAR(30)
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;

CREATE TABLE REPAIR_SERVICE (
		ID_repair_service INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
		name VARCHAR(30), 
		address VARCHAR(30), 
		ID_gear_type INTEGER,
  		ID_team INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;

CREATE TABLE SICK_DAY_CLAIM (
		ID_sick_day_claim INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  		date_from DATE, 
		date_to DATE, 
		claim_is_valid INTEGER,
		claim_value DOUBLE,
		claim_date DATE,
		return_account_number VARCHAR(100),
		ID_sickness_insurance INTEGER NOT NULL, 
  		ID_team INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;

CREATE TABLE SICKNESS_INSURANCE (
		ID_sickness_insurance INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
		premium_price DOUBLE, 
		daily_compensation DOUBLE, 
		ID_policy_product INTEGER NOT NULL,
  		ID_team INTEGER NOT NULL
	)
	DATA CAPTURE NONE 
	COMPRESS NO
	ORGANIZE BY ROW;
  
  
--  -----------------------------FOREIN KEY CREATION---------------------------------

ALTER TABLE CLIENT ADD CONSTRAINT clien_occupatio_FK FOREIGN KEY
	(ID_occupation)
	REFERENCES OCCUPATION
	(ID_occupation)
	ON DELETE CASCADE;
	
ALTER TABLE CLIENT ADD CONSTRAINT clien_team_FK FOREIGN KEY
	(ID_team)
	REFERENCES TEAM
	(ID_team)
	ON DELETE CASCADE;
	
ALTER TABLE CONTRACT ADD CONSTRAINT contra_pol_porod_FK FOREIGN KEY
	(ID_policy_product)
	REFERENCES POLICY_PRODUCT
	(ID_policy_product)
	ON DELETE CASCADE;
	
ALTER TABLE CONTRACT ADD CONSTRAINT contra_clients_cl_FK FOREIGN KEY
	(ID_clients_client)
	REFERENCES CLIENTS_CLIENT
	(ID_clients_client)
	ON DELETE CASCADE;
	
ALTER TABLE CONTRACT ADD CONSTRAINT contract_team_FK FOREIGN KEY
	(ID_team)
	REFERENCES TEAM
	(ID_team)
	ON DELETE CASCADE;
	
ALTER TABLE CLIENTS_CLIENT ADD CONSTRAINT clients_cl_team_FK FOREIGN KEY
	(ID_team)
	REFERENCES TEAM
	(ID_team)
	ON DELETE CASCADE;

ALTER TABLE FILE ADD CONSTRAINT file_contract__FK FOREIGN KEY
	(ID_contract)
	REFERENCES CONTRACT
	(ID_contract)
	ON DELETE CASCADE;
  
ALTER TABLE FILE ADD CONSTRAINT file_gear__FK FOREIGN KEY
	(ID_gear)
	REFERENCES GEAR
	(ID_gear)
	ON DELETE CASCADE;
  
ALTER TABLE FILE ADD CONSTRAINT file_gear_claim__FK FOREIGN KEY
	(ID_gear_claim)
	REFERENCES GEAR_CLAIM
	(ID_gear_claim)
	ON DELETE CASCADE;
  
ALTER TABLE FILE ADD CONSTRAINT file_sick_day_claim__FK FOREIGN KEY
	(ID_sick_day_claim)
	REFERENCES SICK_DAY_CLAIM
	(ID_sick_day_claim)
	ON DELETE CASCADE;
  
ALTER TABLE FILE ADD CONSTRAINT file_liability__FK FOREIGN KEY
	(ID_liability_claim)
	REFERENCES LIABILITY_CLAIM
	(ID_liability_claim)
	ON DELETE CASCADE;

ALTER TABLE FILE ADD CONSTRAINT file_team_FK FOREIGN KEY
	(ID_team)
	REFERENCES TEAM
	(ID_team)
	ON DELETE CASCADE;

ALTER TABLE GEAR ADD CONSTRAINT gear_gear_type_FK FOREIGN KEY
	(ID_gear_type)
	REFERENCES GEAR_TYPE
	(ID_gear_type)
	ON DELETE CASCADE;
	
ALTER TABLE GEAR ADD CONSTRAINT gear_policies_p_FK FOREIGN KEY
	(ID_policy_product)
	REFERENCES POLICY_PRODUCT
	(ID_policy_product)
	ON DELETE CASCADE;

ALTER TABLE GEAR ADD CONSTRAINT gear_team_FK FOREIGN KEY
	(ID_team)
	REFERENCES TEAM
	(ID_team)
	ON DELETE CASCADE;
  
ALTER TABLE GEAR_CLAIM ADD CONSTRAINT gear_repair_se_FK FOREIGN KEY
	(ID_repair_service)
	REFERENCES REPAIR_SERVICE
	(ID_repair_service)
	ON DELETE CASCADE;

ALTER TABLE GEAR_CLAIM ADD CONSTRAINT gear_c_claim_t_FK FOREIGN KEY
	(ID_claim_type)
	REFERENCES CLAIM_TYPE
	(ID_claim_type)
	ON DELETE CASCADE;

ALTER TABLE GEAR_CLAIM ADD CONSTRAINT gear_claim_ge_FK FOREIGN KEY
	(ID_gear)
	REFERENCES GEAR
	(ID_gear)
	ON DELETE CASCADE;
  
ALTER TABLE GEAR_CLAIM ADD CONSTRAINT gear_claim_team_FK FOREIGN KEY
	(ID_team)
	REFERENCES TEAM
	(ID_team)
	ON DELETE CASCADE;
	
ALTER TABLE GEAR_TYPE ADD CONSTRAINT gear_type_team_FK FOREIGN KEY
	(ID_team)
	REFERENCES TEAM
	(ID_team)
	ON DELETE CASCADE;

ALTER TABLE LIABILITY ADD CONSTRAINT liab_policies_p_FK FOREIGN KEY
	(ID_policy_product)
	REFERENCES POLICY_PRODUCT
	(ID_policy_product)
	ON DELETE CASCADE;

ALTER TABLE LIABILITY ADD CONSTRAINT liability_team_FK FOREIGN KEY
	(ID_team)
	REFERENCES TEAM
	(ID_team)
	ON DELETE CASCADE;

ALTER TABLE LIABILITY_CLAIM ADD CONSTRAINT liability__liab_FK FOREIGN KEY
	(ID_liability)
	REFERENCES LIABILITY
	(ID_liability)
	ON DELETE CASCADE;
	
	
ALTER TABLE LIABILITY_CLAIM ADD CONSTRAINT liability_cl_team_FK FOREIGN KEY
	(ID_team)
	REFERENCES TEAM
	(ID_team)
	ON DELETE CASCADE;


ALTER TABLE MONTHLY_PAYMENT ADD CONSTRAINT monthly_pa_client_FK FOREIGN KEY
	(ID_client)
	REFERENCES CLIENT
	(ID_client)
	ON DELETE CASCADE;
	
	
ALTER TABLE MONTHLY_PAYMENT ADD CONSTRAINT monthly_pa_team_FK FOREIGN KEY
	(ID_team)
	REFERENCES TEAM
	(ID_team)
	ON DELETE CASCADE;

	
ALTER TABLE OCCUPATION ADD CONSTRAINT occupation_team_FK FOREIGN KEY
	(ID_team)
	REFERENCES TEAM
	(ID_team)
	ON DELETE CASCADE;
	
ALTER TABLE POLICY_PRODUCT ADD CONSTRAINT policies_p_client_FK FOREIGN KEY
	(ID_client)
	REFERENCES CLIENT
	(ID_client)
	ON DELETE CASCADE;

ALTER TABLE POLICY_PRODUCT ADD CONSTRAINT policies_p_prod_FK FOREIGN KEY
	(ID_product)
	REFERENCES PRODUCT
	(ID_product)
	ON DELETE CASCADE;

ALTER TABLE POLICY_PRODUCT ADD CONSTRAINT policy_prod_team_FK FOREIGN KEY
	(ID_team)
	REFERENCES TEAM
	(ID_team)
	ON DELETE CASCADE;
	
ALTER TABLE REPAIR_SERVICE ADD CONSTRAINT repair_service_gear_type_FK FOREIGN KEY
	(ID_gear_type)
	REFERENCES GEAR_TYPE
	(ID_gear_type)
	ON DELETE CASCADE;
	
ALTER TABLE REPAIR_SERVICE ADD CONSTRAINT repair_serv_team_FK FOREIGN KEY
	(ID_team)
	REFERENCES TEAM
	(ID_team)
	ON DELETE CASCADE;

ALTER TABLE SICK_DAY_CLAIM ADD CONSTRAINT sick_sickness_i_FK FOREIGN KEY
	(ID_sickness_insurance)
	REFERENCES SICKNESS_INSURANCE
	(ID_sickness_insurance)
	ON DELETE CASCADE;

ALTER TABLE SICK_DAY_CLAIM ADD CONSTRAINT sick_team_FK FOREIGN KEY
	(ID_team)
	REFERENCES TEAM
	(ID_team)
	ON DELETE CASCADE;

ALTER TABLE SICKNESS_INSURANCE ADD CONSTRAINT sickness_polici_FK FOREIGN KEY
	(ID_policy_product)
	REFERENCES POLICY_PRODUCT
	(ID_policy_product)
	ON DELETE CASCADE;

ALTER TABLE SICKNESS_INSURANCE ADD CONSTRAINT sick_ins_team_FK FOREIGN KEY
	(ID_team)
	REFERENCES TEAM
	(ID_team)
	ON DELETE CASCADE;
	
	
INSERT INTO PRODUCT (ID_product, product) VALUES (1,'Contracts product');
INSERT INTO PRODUCT (ID_product, product) VALUES (2,'Sick days product');
INSERT INTO PRODUCT (ID_product, product) VALUES (3,'Gear product');
INSERT INTO PRODUCT (ID_product, product) VALUES (4,'Liability product');


INSERT INTO CLAIM_TYPE (ID_claim_type, claim_type, ID_team) VALUES (1,'Damaged', 1);
INSERT INTO CLAIM_TYPE (ID_claim_type, claim_type, ID_team) VALUES (2,'Broken', 1);
INSERT INTO CLAIM_TYPE (ID_claim_type, claim_type, ID_team) VALUES (3,'Stolen', 1);
INSERT INTO CLAIM_TYPE (ID_claim_type, claim_type, ID_team) VALUES (4,'Unrepearable', 1);



	
	